[package]
name = "rust_index"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_index"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.26", features = ["extension-module"] }
rayon = "1.10"

[profile.release]
lto = "fat"
codegen-units = 1
