[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "rust-index"
version = "0.1.0"
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
